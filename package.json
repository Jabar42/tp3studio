{
  "name": "astro-temp",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy",
    "deploy:chat": "wrangler deploy --config wrangler-chat.jsonc src/workers/chat-agent.ts"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.6.1",
    "@astrojs/react": "^5.0.7",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.3",
    "@cloudflare/ai-chat": "^0.8.4",
    "@tp3/chat-widget": "^0.1.3",
    "agents": "^0.15.0",
    "ai": "^6.0.199",
    "astro": "^6.4.2"
  },
  "devDependencies": {
    "wrangler": "^4.97.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
