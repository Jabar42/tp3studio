<section class="comparison section" id="comparacion">
  <div class="container">
    <div class="comparison-header">
      <span class="section-label">La comparación incómoda</span>
      <h2>Mira en qué te gastas tu plata <span class="accent-text">sin pensarlo</span></h2>
    </div>
    <div class="comparison-grid">
      <div class="comp-col comp-col--expenses">
        <div class="comp-col-header"><span class="comp-icon">💸</span><h3>Tus gastos mensuales</h3></div>
        <div class="expense-list">
          <div class="expense-row"><span class="exp-icon">🛵</span><span class="exp-name">Delivery por mes</span><span class="exp-price">~$50</span></div>
          <div class="expense-row"><span class="exp-icon">📱</span><span class="exp-name">Plan de datos</span><span class="exp-price">~$15</span></div>
          <div class="expense-row"><span class="exp-icon">🍺</span><span class="exp-name">Salida un viernes</span><span class="exp-price">~$40</span></div>
          <div class="expense-row"><span class="exp-icon">💇</span><span class="exp-name">Dos peluquerías</span><span class="exp-price">~$30</span></div>
          <div class="expense-row expense-row--worst"><span class="exp-icon">👨‍💻</span><span class="exp-name">"Sobrino que sabe de webs"</span><span class="exp-price">$500+ <small>BOTADOS / MES</small></span></div>
        </div>
        <div class="expense-total"><span class="total-amount">$635+</span><span class="total-label">cada mes, para siempre</span></div>
      </div>
      <div class="comp-vs"><div class="vs-circle"><span>VS</span></div></div>
      <div class="comp-col comp-col--winner">
        <div class="comp-col-header"><span class="comp-icon">🚀</span><h3>Tp3studio</h3></div>
        <div class="winner-price"><span class="winner-amount">$100</span><span class="winner-note">USD · UNA SOLA VEZ · PARA SIEMPRE</span></div>
        <ul class="winner-benefits">
          <li>Web profesional lista en 48h</li><li>Chatbot IA 24/7 incluido</li><li>Diseño responsive personalizado</li><li>Hosting incluido 1 año</li><li>SEO optimizado + carga rápida</li>
        </ul>
        <div class="winner-tagline"><p>La única diferencia es que esta inversión <strong>te devuelve plata cada día</strong>.</p></div>
        <div class="winner-testimonial"><p>"Es más barato de lo que pensaba"</p></div>
      </div>
    </div>
  </div>
</section>

<style>
.comparison { background: linear-gradient(180deg, var(--color-bg) 0%, rgba(245,240,225,0.4) 100%); }
.comparison-header { text-align: center; max-width: 34rem; margin: 0 auto 3rem; }
.comparison-grid { display: grid; grid-template-columns: 1fr; gap: 1rem; align-items: start; max-width: 56rem; margin: 0 auto; }
.comp-col { background: #fff; border-radius: var(--radius-xl); padding: 2rem; border: 2px solid var(--color-border); }
.comp-col-header { display: flex; align-items: center; gap: 0.5rem; margin-bottom: 1.5rem; }
.comp-col-header h3 { font-size: 1.25rem; margin: 0; }
.comp-icon { font-size: 1.5rem; }
.expense-list { display: flex; flex-direction: column; gap: 0.625rem; margin-bottom: 1.25rem; }
.expense-row { display: grid; grid-template-columns: 2rem 1fr auto; align-items: center; gap: 0.6rem; padding: 0.5rem 0.75rem; border-radius: var(--radius-sm); transition: background 200ms; }
.expense-row:hover { background: rgba(220,38,38,0.03); }
.exp-icon { font-size: 1.15rem; }
.exp-name { font-size: 0.875rem; font-weight: 500; }
.exp-price { font-weight: 800; font-size: 0.9rem; color: var(--color-destructive); }
.expense-row--worst { background: rgba(220,38,38,0.04); border: 1px dashed rgba(220,38,38,0.2); }
.expense-row--worst .exp-price small { display: block; font-size: 0.6rem; font-weight: 800; text-transform: uppercase; color: var(--color-destructive); opacity: 0.7; }
.expense-total { padding-top: 1.25rem; border-top: 2px solid rgba(220,38,38,0.15); text-align: center; }
.total-amount { display: block; font-family: var(--font-heading); font-size: 2.5rem; font-weight: 700; color: var(--color-destructive); }
.total-label { font-size: 0.8125rem; color: rgba(0,0,0,0.4); font-weight: 500; }
.comp-vs { display: flex; justify-content: center; align-items: center; padding: 0.5rem 0; }
.vs-circle { width: 3.5rem; height: 3.5rem; border-radius: 50%; background: var(--color-deep); color: #fff; display: flex; align-items: center; justify-content: center; font-family: var(--font-heading); font-weight: 800; font-size: 0.85rem; box-shadow: var(--shadow-md); }
.comp-col--winner { background: linear-gradient(160deg, rgba(255,251,235,0.8), rgba(245,240,225,0.6)); border: 2px solid var(--color-terracotta); box-shadow: 0 0 0 6px rgba(198,123,92,0.06); }
.winner-price { text-align: center; padding: 1.5rem; margin-bottom: 1rem; background: rgba(198,123,92,0.06); border-radius: var(--radius-lg); }
.winner-amount { display: block; font-family: var(--font-heading); font-size: 3.5rem; font-weight: 700; color: var(--color-amber); line-height: 1; }
.winner-note { font-size: 0.6875rem; font-weight: 800; text-transform: uppercase; letter-spacing: 0.05em; color: var(--color-success); margin-top: 0.35rem; display: block; }
.winner-benefits { list-style: none; padding: 0; margin: 0 0 1.25rem; display: grid; gap: 0.5rem; }
.winner-benefits li { font-size: 0.875rem; font-weight: 500; padding-left: 1.6rem; position: relative; }
.winner-benefits li::before { content: ''; position: absolute; left: 0; top: 7px; width: 14px; height: 14px; background: var(--color-success); border-radius: 50%; }
.winner-tagline { text-align: center; margin-bottom: 1rem; font-size: 0.9375rem; }
.winner-testimonial { background: rgba(198,123,92,0.08); border-radius: var(--radius-md); padding: 0.75rem 1.25rem; text-align: center; }
.winner-testimonial p { margin: 0; font-size: 0.875rem; font-style: italic; color: var(--color-clay); font-weight: 500; }
@media (min-width: 768px) { .comparison-grid { grid-template-columns: 1fr auto 1fr; gap: 0; } .comp-col { padding: 2.25rem; } }
</style>
